(ns tanara.models.dominio.dominio
  (:use [clojure.xml :only (parse)])) ; <- me ignore por enquanto...

;(use '[clojure.xml :only (parse)]) ;biblioteca de manipulação de xml do clojure! :D
(def estrutura-dominio (parse "src/tanara/models/dominio2/dominio.xml")) ;xml não esta na pasta raiz, coloque diretório completo.

